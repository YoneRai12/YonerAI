[project]
name = "yonerai-cli"
version = "0.5.0"
description = "YonerAI local CLI runtime"
requires-python = ">=3.11"
dependencies = [
    "cryptography>=42,<47",
]

[project.scripts]
yonerai = "yonerai_cli.cli:main"

[tool.setuptools.packages.find]
where = ["."]
