[project]
name = "yonerai-cli"
version = "0.2.0a1"
description = "YonerAI local CLI runtime alpha"
requires-python = ">=3.11"
dependencies = [
    "cryptography>=42,<47",
]

[project.scripts]
yonerai = "yonerai_cli.cli:main"

[tool.setuptools.packages.find]
where = ["."]
