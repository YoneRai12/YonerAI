# Deprecated Legacy Changelog

This legacy ORA-era changelog is not the current YonerAI release record.

Use the current public release/checkpoint docs under `docs/releases/` and the
current phase context instead. This tombstone is retained only to avoid exposing
stale internal notes as a public product history.
