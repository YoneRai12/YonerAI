# Deprecated Migration Snapshot

This legacy migration note is no longer a public YonerAI runbook.

The previous content was tied to old local-machine workflows and stale product
framing. It has been replaced with this tombstone so the public repository does
not present obsolete instructions as current guidance.

Use `README.md`, `README_JP.md`, and `docs/CURRENT_PHASE_CONTEXT.md` for current
public-safe direction.
