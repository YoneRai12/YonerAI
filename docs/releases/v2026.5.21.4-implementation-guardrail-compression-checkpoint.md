# v2026.5.21.4 Implementation Guardrail Compression Checkpoint

Date: 2026-05-21

## Summary

This checkpoint records the v7.7 implementation-first guardrail compression pass. The pass moved several public-safe guardrails from broad documentation into concrete current-main tests, a small security/runtime patch, and acceptance harnesses.

It is not a production release, not a deploy, not Discord restoration, not `src/cogs/ora.py` resolution, and not a v7.8 start.

## Implemented

- Restored `/say` owner/admin boundary enforcement on current `main` through `can_use_say_command()`.
- Added regression coverage that keeps `/say` owner-only and prevents direct channel-send behavior from returning.
- Expanded synthetic Discord Hybrid/Self-Host signed-contract acceptance tests for responder policy, same-message edit ordering, controlled errors, node mode consistency, and public-safe payload fields.
- Added a three-mode acceptance harness for Full Private Self-Host, Official Hybrid Private, and Official Managed Cloud capability posture.
- Added a follow-up test strengthening the three-mode fixture so available, gated, and disabled categories must remain aligned across all three modes.
- Pinned existing `src/cogs/ora.py` pure helper behavior with AST-based tests that do not import Discord runtime or modify the file.

## Security and Boundary

- Old PR #129 was closed as superseded by the fresh current-main `/say` patch.
- Signed Discord gateway envelopes remain contract inputs and do not imply trust.
- Dangerous capabilities remain deny-by-default in the public capability harness.
- Public PythonBot / old ORA residue is not promoted to a production responder.
- `src/cogs/ora.py` remains unchanged; this checkpoint adds extraction planning and pure-helper contract tests only.
- `reference_clawdbot` remains untouched.

## Discord Contract Status

- Synthetic contract tests now cover private-gateway-only response policy, terminal once-only behavior, controlled error shape, same-message edit references, public-safe payload rejection, and node mode consistency.
- This is still synthetic evidence. It does not connect to live Discord, does not require a Discord token, and does not restore Discord chat.

## Tests

- `python -m pytest tests/test_say_command_access_policy.py tests/test_voice_listen_access_policy.py -q`
- `python -m pytest tests/test_discord_hybrid_gateway_contract.py -q`
- `python -m pytest tests/test_phase5_three_mode_same_experience_acceptance.py tests/test_public_capability_boundary.py -q`
- `python -m pytest tests/test_ora_pure_helper_contract.py -q`
- `python -m pytest tests/test_public_runnable_smoke.py tests/test_runtime_env_loader.py -q`
- targeted `ruff`, `compileall`, `git diff --check`, and changed-line public-surface scans
- GitHub Actions passed for the merged PRs in this checkpoint stream.

## Not Included

- production readiness
- shipping completion
- official cloud completion
- live operations completion
- full product completion
- Discord gateway completion or live Discord chat
- persistent memory completion
- Google login
- final Web UI
- unrestricted shell or dynamic MCP execution
- Tools/MCP completion
- `src/cogs/ora.py` resolution
- v7.8 start or completion

## Still Open

- Remaining stale security/runtime PRs beyond #129.
- Dependency PR lane drain.
- Physical root cleanup beyond already validated moves.
- Real private Discord gateway implementation and live acceptance evidence.
- `src/cogs/ora.py` first behavior-preserving extraction PR.
- README_JP broader mojibake restoration as a dedicated public-text cleanup lane.

## Traceability

- PR #233: guardrail compression policy.
- PR #234: `/say` current-main security/runtime patch.
- PR #235: Discord hybrid contract acceptance expansion.
- PR #236: `src/cogs/ora.py` boundary extraction plan.
- PR #237: three-mode same-experience acceptance harness.
- PR #238: three-mode harness review follow-up.
- PR #239: ORA pure helper contract tests.
