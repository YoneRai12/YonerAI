Released: 2026-04-11

YonerAI continues date-based versioning (`YYYY.M.D`).

# 日本語

## リリースの焦点
このリリースは、公開配布向け YonerAI ノード側の Distribution Node MVP update です。
配布ノードを安全に更新し、安全に最小実行するための基盤を入れています。
これは実挙動が入った review-checkpoint release であり、完成宣言ではありません。
また、sensitive runtime の着地でも、公式クラウド全部入り通信基盤の完成宣言でもありません。

## このパッチでできるようになったこと
- 署名付き release verification の fail-closed 基盤
  - release manifest
  - signed metadata
  - provenance record
  - freshness expiry
  - rollback check
- capability manifest の deny-only 運用
  - `schema_version = yonerai-distribution-capabilities/v1`
  - `profile = distribution_node_mvp`
  - `default_action = deny` only
- Internal Run API v0.1 の最小契約
  - `POST /v1/messages`
  - `GET /v1/runs/{run_id}/events`
  - `POST /v1/runs/{run_id}/results`
- run API の file-ref-only 化
- files API 経由の controlled download / short-lived URL
- Distribution Node mode での owner-scoped file / run-event / run-result access
- band2 / SSE hardening
- distribution file tables の Alembic migration support

## 検証
- Alembic migration flow のローカル検証を完了
  - `python -m alembic upgrade head`
  - `python -m alembic downgrade -1`
- Distribution Node MVP 関連テストはローカル検証で green

## 含まれないもの
このリリースには以下は含まれません。
- `src/cogs/ora.py` landing
- sensitive runtime landing
- arbitrary shell support
- arbitrary SQL support
- arbitrary file write support
- high-risk control-plane execution
- full live operational exactness
- full product completion の主張
- 公式クラウド全部入り通信基盤の完成版

## Truth Anchor
- `docs/DISTRIBUTION_NODE_MVP.md`

# English

## Release Focus
This release packages a narrow Distribution Node MVP update on the public/distributable YonerAI node side.
It establishes the secure baseline required to update and run a minimal Distribution Node safely.
This is a review-checkpoint release with landed and verified behavior, not a completion claim.
It is not a sensitive runtime landing and not a declaration of a full official-cloud complete communication foundation.

## What this patch makes possible
- A fail-closed signed release verification baseline
  - release manifest
  - signed metadata
  - provenance record
  - freshness expiry
  - rollback check
- Deny-only capability behavior
  - `schema_version = yonerai-distribution-capabilities/v1`
  - `profile = distribution_node_mvp`
  - `default_action = deny` only
- The narrow Internal Run API v0.1 contract
  - `POST /v1/messages`
  - `GET /v1/runs/{run_id}/events`
  - `POST /v1/runs/{run_id}/results`
- File-ref-only run APIs
- Controlled downloads through the files API with short-lived URLs
- Owner-scoped file / run-event / run-result access in Distribution Node mode
- band2 / SSE hardening
- Alembic migration support for distribution file tables

## Validation
- Local validation completed for the Alembic migration flow
  - `python -m alembic upgrade head`
  - `python -m alembic downgrade -1`
- Relevant Distribution Node MVP tests are green in local validation

## Not Included
This release does not include:
- `src/cogs/ora.py` landing
- sensitive runtime landing
- arbitrary shell support
- arbitrary SQL support
- arbitrary file write support
- high-risk control-plane execution
- full live operational exactness
- a full product completion claim
- a full official-cloud complete communication foundation

## Truth Anchor
- `docs/DISTRIBUTION_NODE_MVP.md`

## Release Tag
- `v2026.4.11`
