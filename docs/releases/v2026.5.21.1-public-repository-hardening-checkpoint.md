# YonerAI v2026.5.21.1 Public Repository Hardening Checkpoint

Status: public repository hardening checkpoint. Not production, not deploy, not full product completion, and not a v7.8 start.

## Summary

This checkpoint aligns the GitHub Release stream with the current v7.7 public repository hardening work after the earlier `v2026.5.20.14` markdown-only checkpoint. It records public PR/branch reality, file-to-PR traceability, large-codebase integration mapping, a current-main local LLM access hardening patch, dependency lane triage, and a reference-backed root no-move decision.

The verified local date for this checkpoint is 2026-05-21 JST. Existing `v2026.5.21` already exists, so this checkpoint uses the same-day suffix tag `v2026.5.21.1`.

## Open

- Remaining security/runtime PRs that touch media, dashboard, Discord, or private-adjacent paths still need dedicated reproduction or owner decision.
- Dependency PRs remain open where security value or compatibility risk is uncertain.
- Root launchers, `config.yaml`, `main.py`, and compose files remain root-visible until dedicated migration lanes validate references.
- `README_JP.md` still needs a separate UTF-8 restoration lane; this checkpoint does not rewrite the Japanese first screen.
- `src/cogs/ora.py` remains an explicit do-not-touch boundary for this work.

## Closed

- PR #206 was closed as superseded by the current-main local LLM public access gate patch and tests.
- PR #207 was closed as superseded by the same current-main local LLM hardening patch and tests.

## Changed

- Added a current PR and branch reality ledger for the public GitHub state.
- Added a file-to-PR traceability matrix so GitHub root entries have durable context without mass-touching files.
- Refined the large-codebase inventory and v7.7 integration/retirement lane board.
- Hardened the public local LLM path so local LLM mode requires explicit opt-in and a public-token gate before use.
- Rechecked dependency PR lanes and documented why no dependency PR was safe to close blindly.
- Documented that no additional root physical move is safe without launcher/config/entrypoint-specific migration work.

## Security And Hygiene

- Local LLM public access now requires `ORA_LOCAL_LLM_ENABLED=1` plus the configured public-token header when the public message path selects local LLM mode.
- Request-controlled local provider base URL behavior was narrowed so the public message path does not pass request-derived base URLs into provider configuration.
- Root cleanup is evidence-backed rather than cosmetic: active or uncertain entrypoints were left in place.
- Dependency PR triage confirms there were zero open Dependabot alerts at the time of the lane recheck, but it does not claim the dependency backlog is resolved.

## Repository Presentation

- Current GitHub Release visibility moves forward from `v2026.5.20.6` to this date-correct suffix checkpoint once the release object is created.
- README and README_JP point at this checkpoint as the latest public checkpoint note.
- Historical future-dated markdown artifacts remain traceability records and are not deleted or retagged.

## Validation

Local validation across the merged lanes included:

- `git diff --check`
- `python -m pytest tests/test_public_core_message_mvp.py tests/test_local_llm_provider.py tests/test_surface_api_run_contract.py -q`
- `python -m pytest tests/test_public_runnable_smoke.py tests/test_runtime_env_loader.py -q`
- `python -m ruff check core/src/ora_core/api/routes/public_messages.py core/src/ora_core/providers/local_llm.py tests/test_public_core_message_mvp.py tests/test_local_llm_provider.py tests/test_surface_api_run_contract.py`
- `python -m compileall core/src/ora_core/api/routes/public_messages.py core/src/ora_core/providers/local_llm.py`
- changed-file secret scans
- changed-file local absolute path / username / hostname scans
- mojibake and hidden Unicode scans on changed docs
- confirmation that `src/cogs/ora.py` was not changed
- confirmation that `reference_clawdbot` was not initialized, repaired, removed, replaced, staged, or edited

GitHub checks on the merged PRs passed for `core-test` and `build-and-test (3.11)`.

## Not Included

This checkpoint does not include production readiness, shipping completeness, official-cloud completion, live-ops completion, full product completion, hybrid completion, persistent memory, Google login, Discord gateway completion, provider ecosystem completion, final Web UI completion, Tools/MCP completion, ChatGPT-equivalent parity, Pass 2 landing, all security backlog resolution, all dependency backlog resolution, a v7.8 start, or `src/cogs/ora.py` resolution.

No deploy, production signing key, production trust store, production DB behavior, real telemetry collection, raw prompt/completion ingestion, unrestricted shell/tool execution, or external provider live generation was added.

## Still Open

- Security/runtime deep pass for PRs such as #128, #133, #60, #131, #132, #135, #129, #134, and newer owner-created security PRs where applicable.
- Dependency lanes for GitHub Actions, Python runtime, Discord/crypto, provider/media, optional memory/vector, and web/client dependencies.
- Root launcher/config/entrypoint migration pilot with reference updates and smoke tests.
- README_JP UTF-8 restoration and broader public repository presentation pass.
- v7.7 implementation evidence ledger and v7.8 readiness decision.

## Traceability

- PR #219: current PR and branch reality ledger, merge `7f747a481150e46ae86ec1b9303c2d90f303ed40`.
- PR #220: file-to-PR traceability matrix, merge `2694503a463d52d6a2c12df72f10d394ab3de2da`.
- PR #221: large-codebase integration and retirement map, merge `146d2b5948e05775786a14d5345e192e7ce5e8f2`.
- PR #222: local LLM public access gate hardening, merge `d977ab45af3957e2a7fb8a2483de975ef1c753e5`.
- PR #223: dependency lane triage recheck, merge `a4cbbdd828a3196e30fc25728a399cefc8b506d7`.
- PR #224: root physical cleanup no-move decision, merge `6fe30e16cca6542b85c5ae67bd5e39486f9afb92`.
