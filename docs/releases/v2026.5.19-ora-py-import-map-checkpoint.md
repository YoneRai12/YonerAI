# YonerAI v2026.5.19 `ora.py` Import Map Checkpoint

Date: 2026-05-19

Status: refactor Step 0 checkpoint

Not a production release

## Open

`src/cogs/ora.py` remains unresolved boundary residue. This checkpoint starts mainline refactor preparation without changing runtime behavior.

## Closed

- Static import map tooling was added for `src/cogs/ora.py`.
- Facade contract tests were added for the current `ORACog` static surface.
- Known direct inbound references were pinned.
- Duplicate method-name detection was added as a future cleanup signal.

## Changed

- Future refactor PRs now have a no-runtime-import test baseline.
- The next extraction can compare against a deterministic static surface.

## Security and hygiene

- No secrets are required.
- No local runtime path is required.
- No Discord token is required.
- No provider API key is required.
- No private repository is required.

## Not included

This checkpoint does not include:

- `src/cogs/ora.py` implementation changes
- `src/cogs/ora.py` rename
- runtime split implementation
- Web chat implementation
- Google login implementation
- memory synchronization
- Discord gateway completion
- API / CLI / native Japanese CLI / SNS implementation
- deployment
- release tag creation
- production readiness

## Still open

- Extract tool catalog behavior in a later PR.
- Extract policy and approval wrappers in a later PR.
- Classify SafeShell and host-control boundaries before moving code.
- Keep private/control-plane implementation out of public artifacts.

## Next

Run `refactor/ora-py-tool-catalog` or `refactor/ora-py-policy-boundary` only after this Step 0 checkpoint is merged and green.
