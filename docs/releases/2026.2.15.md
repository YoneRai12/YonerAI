Released: 2026-02-15

YonerAI continues date-based versioning (`YYYY.M.D`).

## Highlights (JP)
- **Discord: Web検索ツールの不具合修正**:
  - `web_search` が ToolHandler で未ルーティングになっていた問題を修正
  - `web_action` は Admin Only を明示し、誤公開を防止
- **Discord: 音楽再生の不具合修正**:
  - `music_play` 等が常に `Music system not accessible` になる経路を修正
  - ORACog 経由で既存の `MusicSkill` を正しく参照するように変更
- **Core: SSE イベントの取りこぼし対策**:
  - SSE クライアント接続が遅れた場合でも、先行イベントをバッファして配信
  - 「完了済みなのに events が来なくて待ち続ける」ケースを回避

## Highlights (EN)
- **Discord: Web search tool routing fix**:
  - Fixed `web_search` not being dispatched by ToolHandler
  - `web_action` is explicitly Admin Only to avoid accidental exposure
- **Discord: Music playback fix**:
  - Fixed wrappers that always returned `Music system not accessible`
  - Correctly reuses the existing `MusicSkill` instance via ORACog
- **Core: Prevent missing SSE events**:
  - Buffers early events until the first SSE listener attaches
  - Avoids clients hanging forever when the run already completed

## New / Updated Env Keys (not exhaustive)
- None

## Release Tag
- `v2026.2.15`

