# YonerAI v2026.5.20.7 Surface API Run Contract Checkpoint

Date: 2026-05-20

Status: public checkpoint note. Not a production release.

## Open

- Advance the v7.7 surface ladder from API contract authority toward CLI, native Japanese CLI, Web capability manifest, and SNS claim guardrails in separate lanes.
- Keep old broad Web, Discord, deploy, memory, and private/control-plane work out of this API lane.

## Closed

- Added a public Core Surface API 0.1 run-oriented smoke contract:
  - `POST /api/v1/agent/run`
  - `GET /api/v1/agent/runs/{run_id}/events`
  - `POST /api/v1/agent/runs/{run_id}/results`
- Kept the run state in-memory and public-safe.
- Preserved mock/offline determinism and loopback-only local LLM enforcement through the existing public message path.
- Re-evaluated PR #142 and applied a fresh narrow access-gate fix for current main:
  - protected `/v1/messages`
  - protected `/v1/runs/{run_id}/events`
  - protected `/v1/runs/{run_id}/results`
  - protected `/v1/files/{file_id}/download-url`
  - kept `/v1/files/download/{ticket}` ticket-based access outside the core token dependency.

## Changed

- Updated the external agent API contract to distinguish canonical `/v1/*` run contract from Surface API 0.1 alias behavior.
- Updated the MVP capability matrix and README capability text for the new local run smoke surface.

## Security and Hygiene

- `ORA_CORE_API_TOKEN`, when configured, gates the new Surface API endpoints.
- Without a token, Core access remains loopback-only.
- Submitted Surface API results are accepted only as public-safe in-memory metadata with `trusted: false`.
- No persistent memory, deployment, production signing, production trust store, Google login, external provider live generation, full hybrid connector, or production DB behavior was added.

## Not Included

- Production-ready API.
- Official cloud completion.
- Full hybrid connector.
- Persistent memory.
- Google login.
- Discord gateway completion.
- Final Web UI.
- CLI implementation.

## Still Open

- Surface CLI 0.1 smoke client.
- Native Japanese CLI confirmation contract.
- Web surface capability manifest.
- Growth/SNS claim guardrails.
- Deeper security review for old broad security PRs.

## Next

- Start `surface-cli-0.1` in a separate branch only after the Surface API PR is merged and checks are green.
