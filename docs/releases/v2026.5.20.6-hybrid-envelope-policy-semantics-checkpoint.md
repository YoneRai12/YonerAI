# YonerAI v2026.5.20.6 Hybrid Envelope Policy Semantics Checkpoint

Date: 2026-05-20

Status: public checkpoint note. Not a production release.

## Open

- Fix post-merge hybrid donation policy semantics so stricter policy cannot be bypassed by spoofing `data_class`.
- Keep signed-envelope donation policy quarantine-first and approval-gated.

## Closed

- Memory-candidate policy now triggers from memory-candidate semantics, not only `data_class == "memory_candidate"`.
- Improvement-proposal payload validation now triggers from proposal semantics, not only `data_class == "improvement_proposal"`.
- Regression tests cover spoofed `data_class` with memory-candidate and improvement-proposal envelope/capability/purpose semantics.

## Changed

- Added conservative semantic predicates for hybrid envelope policy classification.
- Added regression tests proving memory and proposal policy checks still run when `data_class` is spoofed to another allowed value.

## Security and hygiene

- A valid signature still does not make donated data trusted.
- Memory candidates remain quarantine-first and must not persist by default.
- Improvement proposals still require safety evidence and must not auto-mutate code.
- No production signing keys, production trust store, deploy path, persistent memory, Google login, or external provider live generation was added.

## Not included

- No production-ready claim.
- No official-cloud completion claim.
- No hybrid completion claim.
- No persistent memory completion.
- No full hybrid connector.
- No deploy.
- No `src/cogs/ora.py` resolution.

## Still open

- Production key lifecycle and durable replay protection remain future lanes.
- Root helper movement/retirement remains blocked until the policy fix is merged or explicitly stopped.
- Memory persistence remains a separate owner-approved design decision.

## Next

- Merge the narrow policy semantics fix after CI and review are clean.
- Only then continue root helper retirement validation from a fresh main branch.
