# VPS Deployment Note

Status: moved out of the public runnable MVP path.

This public repository does not publish the official VPS deployment recipe, live
route map, tunnel configuration, or control-plane operations detail.

The public runnable MVP is limited to local, credential-free Core API smoke
checks. VPS deployment, supervision, rollback, tunnel ownership, and official
runtime orchestration belong in the private runtime and control-plane lanes.

Do not treat this file as deployment documentation.
