# Deprecated Delegation Plan

This older delegation plan is not current public YonerAI source-of-truth.

The previous content included local-machine assumptions and stale legacy
framing. It has been replaced with this tombstone so public readers do not treat
it as an active implementation plan.

Use `AGENTS.md`, `docs/CURRENT_PHASE_CONTEXT.md`, and the current task-specific
docs for active guidance.
