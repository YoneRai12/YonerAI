# YonerAI Checkpoint Compatibility Directory

Prefer `docs/changelog/checkpoints/` for new checkpoint logs.

This directory exists for short compatibility notes or redirects when a previous workflow expects `docs/checkpoints/`. Do not use it as a reason to create GitHub Releases for internal checkpoints.
