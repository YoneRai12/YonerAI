# yonerai.com/releases/v0.10.0-alpha.1 content foundation

Status: content contract for the future
`https://yonerai.com/releases/v0.10.0-alpha.1` page. GitHub Release remains the
distribution source of truth. This page must not be treated as deployed until
the site deployment is separately verified.

## Title

YonerAI v0.10.0-alpha.1 Public Orchestration Boundary

## Summary

v0.10.0-alpha.1 collects the public post-v0.9 orchestration work: TUI status
navigation, dry-run Google auth boundary hardening, broader Quality Wall scans,
and plan-only installer/site alignment for the current prerelease.

It does not enable production Official Managed Cloud, production Oracle,
production Google login, OpenAI shared traffic, live Discord, production
signing/trust, npm/winget distribution, or a production network installer.

## Commands

```powershell
yonerai
yonerai auth google login --dry-run --pretty --lang ja
yonerai privacy status --pretty --lang ja
yonerai manifest verify releases/manifest.v0.10.0-alpha.1.json --pretty
yonerai install plan --manifest releases/manifest.v0.10.0-alpha.1.json --pretty
yonerai update check --manifest releases/manifest.v0.10.0-alpha.1.json --pretty
.\install.ps1
.\install-local.ps1
```

Inside the interactive CLI:

```text
/ホーム
/状態
/設定
/モデル
/ローカルLLM
/提供元
/提供元選択
/安全
/履歴
/認証
/プライバシー
/自己進化
/更新
```

## Installer Boundary

`install.ps1` is plan-only and defaults to
`releases\manifest.v0.10.0-alpha.1.json`. It may read a local manifest and
display artifact, SHA256, signature, and trust status. It does not download,
install, mutate PATH, execute remote code, request admin, edit registry, or
install services.

## Safety Boundary

Google auth is still dry-run/contract only. OpenAI shared traffic remains
disabled by default. Self-evolution remains proposal-only. Local LLM setup is
loopback-only. Provider live calls require explicit live/env opt-in. Hybrid,
Relay, Local Node, and Oracle surfaces remain public-safe local-dev contracts
or stubs.

## Not Included

- Full YonerAI cloud production readiness.
- Production Oracle/control-plane runtime.
- Official Managed Cloud runtime in this public repo.
- Production Google login or token storage.
- OpenAI shared traffic runtime.
- Live Discord restoration.
- Production network installer.
- PATH mutation by default.
- Production signing keys or production trust stores.
- npm or winget distribution.
