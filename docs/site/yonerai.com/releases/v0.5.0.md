# yonerai.com/releases/v0.5.0 content foundation

Status: public content contract for the future
`https://yonerai.com/releases/v0.5.0` page. GitHub Release remains the
distribution source of truth.

## Title

YonerAI CLI Local Runtime v0.5.0

## Summary

v0.5.0 is the first non-prerelease YonerAI CLI Local Runtime release. Users can
download the release ZIP, install the local CLI into a virtual environment, run
`yonerai`, choose a language, chat with the safe default mock provider, open
settings, inspect task progress, see reviewer/subagent planning, review safety
boundaries, and view redacted local run history.

## Primary command

```powershell
yonerai
```

## Safe install command block

```powershell
cd "$HOME\Downloads\YonerAI-0.5.0"
python --version
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -U pip
python -m pip install -r core/requirements.txt httpx
python -m pip install -e clients/cli
yonerai
```

## Manifest commands

```powershell
yonerai manifest verify releases/manifest.v0.5.0.json --pretty
yonerai install plan --manifest releases/manifest.v0.5.0.json --pretty
yonerai update plan --manifest releases/manifest.v0.5.0.json --pretty
```

## Links

- GitHub Release: https://github.com/YoneRai12/YonerAI/releases/tag/v0.5.0
- Release asset: https://github.com/YoneRai12/YonerAI/releases/download/v0.5.0/YonerAI-0.5.0.zip
- Manifest: `releases/manifest.v0.5.0.json`
- License policy: `docs/legal/LICENSE_POLICY.md`

## Not included

- Full YonerAI cloud production readiness.
- Production Oracle/control-plane runtime.
- Official Managed Cloud runtime in this public repo.
- Live Discord restoration.
- Network-executing installer.
- `irm ... | iex`.
- PATH mutation by default.
- Production signing keys or production trust stores.
- npm or winget distribution.
