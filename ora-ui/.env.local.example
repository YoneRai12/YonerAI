# ora-ui/.env.local.example
#
# Copy this file to: ora-ui/.env.local
# - DO NOT commit ora-ui/.env.local (it contains secrets)
#
# This dashboard uses NextAuth (Auth.js). In this project, the Discord provider reads
# the following env vars by convention.

# Auth secret (required in production; recommended in dev too)
# Generate: openssl rand -base64 32
AUTH_SECRET=CHANGE_ME_TO_RANDOM_32B

# Discord OAuth app credentials (Developer Portal)
AUTH_DISCORD_ID=YOUR_DISCORD_CLIENT_ID
AUTH_DISCORD_SECRET=YOUR_DISCORD_CLIENT_SECRET

# Optional: when deployed behind a reverse proxy/tunnel
# AUTH_TRUST_HOST=true

