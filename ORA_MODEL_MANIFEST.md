# Deprecated Legacy Model Manifest

This legacy ORA-era model manifest is not a current YonerAI public contract.

Provider and model behavior must be described through current public-safe
contracts and configuration docs. This tombstone prevents stale routing notes
from being read as active product truth.
